require "spec_helper"

describe Showtimes do
  it "has a version number" do
    expect(Showtimes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
